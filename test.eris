Point :: struct {
    x: i32,
    y: i32,
}


test :: (x : i32) : i32 {
        return x * 2;
}

main :: () : i32 {
    i := Point { 3, 4 };
    o := "Hello World %d\n";
    printf(o, test(32));
    return 0;
}
